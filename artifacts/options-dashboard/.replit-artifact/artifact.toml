kind = "web"
previewPath = "/"
title = "Indian Options Paper Trader"
version = "1.0.0"
id = "artifacts/options-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25612

[services.development]
run = "pnpm --filter @workspace/options-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/options-dashboard", "run", "build" ]
publicDir = "artifacts/options-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25612"
BASE_PATH = "/"
